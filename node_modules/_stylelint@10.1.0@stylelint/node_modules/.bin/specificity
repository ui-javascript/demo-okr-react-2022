#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_specificity@0.4.1@specificity/bin/specificity" "$@"
  ret=$?
else 
  node  "$basedir/../../../_specificity@0.4.1@specificity/bin/specificity" "$@"
  ret=$?
fi
exit $ret

#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_umi-uni18n@1.1.7@umi-uni18n/cli.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_umi-uni18n@1.1.7@umi-uni18n/cli.js" "$@"
  ret=$?
fi
exit $ret

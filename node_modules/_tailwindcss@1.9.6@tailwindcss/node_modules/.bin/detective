#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_detective@5.2.0@detective/bin/detective.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_detective@5.2.0@detective/bin/detective.js" "$@"
  ret=$?
fi
exit $ret

#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_mustache@3.0.1@mustache/bin/mustache" "$@"
  ret=$?
else 
  node  "$basedir/../../../_mustache@3.0.1@mustache/bin/mustache" "$@"
  ret=$?
fi
exit $ret

#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../../_purgecss@2.3.0@purgecss/bin/purgecss" "$@"
  ret=$?
else 
  node  "$basedir/../../../../_purgecss@2.3.0@purgecss/bin/purgecss" "$@"
  ret=$?
fi
exit $ret
